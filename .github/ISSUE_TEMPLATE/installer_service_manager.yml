name: Installer or service-manager problem
description: Report an installer, startup, uninstall, or service-manager failure
title: "[Install]: "
labels:
  - bug
  - installer

body:
  - type: markdown
    attributes:
      value: |
        Use this when the installer, startup service, environment sync, Hyprland bootstrap,
        trial mode, or uninstall behavior fails.

  - type: dropdown
    id: service_manager
    attributes:
      label: Selected service manager
      options:
        - systemd --user
        - dinit --user
        - runit user services
        - s6-rc user services
        - Not sure
    validations:
      required: true

  - type: textarea
    id: environment
    attributes:
      label: Environment
      description: Include the system details needed to reproduce installer behavior.
      placeholder: |
        - Distro:
        - Wayland compositor:
        - Shell:
        - Install command:
        - UNIXNOTIS_SERVICE_MANAGER:
        - UNIXNOTIS_RUNIT_SERVICE_DIR:
        - UNIXNOTIS_S6_DATA_DIR:
        - UNIXNOTIS_S6RC_LIVE_DIR:
    validations:
      required: true

  - type: textarea
    id: failure
    attributes:
      label: What failed?
      description: Describe the failed action and what should have happened instead.
      placeholder: |
        Install failed while ...
        Startup did not ...
        Uninstall refused ...
    validations:
      required: true

  - type: textarea
    id: output
    attributes:
      label: Installer and doctor output
      description: |
        Paste installer output followed by `noticenterctl doctor --verbose`. Doctor reports status for
        every supported backend and only collects persistent logs when systemd journald provides them.
      render: shell
      placeholder: |
        cargo run --release -p unixnotis-installer -- --service-manager ...
        noticenterctl doctor --verbose --service-manager ...
    validations:
      required: true

  - type: textarea
    id: filesystem
    attributes:
      label: Relevant paths
      description: Include service artifact paths or config files mentioned by the installer.
      placeholder: |
        - $HOME/.local/bin:
        - service artifact path:
        - Hyprland config path:
        - shell startup file:

  - type: checkboxes
    id: checks
    attributes:
      label: Checks
      options:
        - label: I searched existing issues before opening this report
          required: true
        - label: I included the selected service manager and install command
          required: true
        - label: I pasted terminal output as text instead of only attaching screenshots
          required: true
        - label: I included `noticenterctl doctor --verbose` output, or explained why it could not run
          required: true
